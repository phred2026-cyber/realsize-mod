{
  "schemaVersion": 1,
  "id": "realsize",
  "version": "${version}",
  "name": "RealSize (Rebuild Groundwork)",
  "description": "Non-deployable structural groundwork for the RealSize clean rebuild.",
  "authors": ["PyreHaven"],
  "contact": {},
  "license": "MIT",
  "icon": "assets/realsize/icon.png",
  "environment": "*",
  "entrypoints": {
    "main": ["xyz.pyrehaven.realsize.RealSizeMod"]
  },
  "mixins": ["realsize.mixins.json"],
  "depends": {
    "fabricloader": "${loader_dependency}",
    "minecraft": "${minecraft_dependency}",
    "java": ">=21",
    "fabric-api": "*"
  }
}
